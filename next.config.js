// next.config.js

module.exports = {
	// Add the following configuration to enable static HTML export
	// and specify the output directory
	output: "/public", // Change '/public' to the desired output directory
};
